import pandas as pd

def get_stats(start_szn, end_szn):
    df = pd.read_csv("team_stats_traditional_rs.csv") # code cant find this.

    filtered_df = df[df['SEASON'].between[start_szn, end_szn]]

    return filtered_df[['TEAM_NAME', 'W', 'L', 'PLUS_MINUS', 'SEASON']]


